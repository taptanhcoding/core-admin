import { AliasToken, ComponentTokenMap } from 'antd/es/theme/interface';
import { ColorPalette } from 'app:constants';

export const Pagination: Partial<ComponentTokenMap['Pagination']> & Partial<AliasToken> = {
  colorText: ColorPalette.SMOKE,
};
