import { AliasToken, ComponentTokenMap } from 'antd/es/theme/interface';
import { ColorPalette } from 'app:constants';

export const Menu: Partial<ComponentTokenMap['Menu']> & Partial<AliasToken> = {
  // radiusItem: 0,
  // colorItemText: ColorPalette.SMOKE,
  // colorActiveBarBorderSize: 0,
  // colorItemBgSelected: `${ColorPalette.BRONZE}1A`,
  // itemMarginInline: 0,
  // controlHeightLG: 48,
  // lineHeight: 20 / 14,
  // padding: 12,
  // marginXXS: 0,
};
